<% count = 0 %>
<div class="joint-politicians clearfix">
<% politician.joint_initiate_bill_politicians[0...4].each_with_index do |tmp, i| -%>
  <% count += 1 %>
  <div class="joint-politician<%= (i==0 || i==2)?" alpha":(i==1 ||i==3)?" omega":""%>">
    <% p = Politician.where(:_id => tmp[0]).first -%>
    <% if p && p.profile_photo? -%>
      <% if p.candidate -%>
        <%= link_to image_tag(p.profile_photo.url(:square100)), district_politician_path(p.id) %>
      <% else -%>
        <%= image_tag(p.profile_photo.url(:square100))%>
      <% end -%>
    <% else -%>
      <%= link_to image_tag("http://placehold.it/48x48"), '#'%>
    <% end -%>

    <div class="joint-info"><%= p.nil? ? tmp[0] : p.name  %>
    <%= "#{(tmp[1].to_f/politician.initiate_bills.count*100.0).round}%" %></div>
  </div>
<% end -%>
<% if politician.joint_initiate_bill_politicians.empty? -%>
  함께 발의한 의원 정보가 없습니다.
<% end -%>
</div>
