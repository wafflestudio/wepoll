<div id="<%= tweet_tweet_replies_path(t) %>" class="tweet_item clearfix <%= c %>">
  <div id="<%= tweet_recommend_path(t) %>" class="count">
    <%= t.recommend_count %>
  </div>
  <div class="content">
    <div>
      <span class="tweet_author"><%= t.name if t.name? %> <%= "@#{t.screen_name}" if t.screen_name? %></span>
      <a id=<%= t.id %> class="reply_btn">댓글 달기</a>
      <%= link_to '공감하기', tweet_recommend_path(t), :class => "tweet_recommend_btn" %>
    </div>
    <div class="tweet_message">
      <%= t.content %>
    </div>
    <div>
      <%= t.created_at %> 댓글<%= t.tweet_replies.count %>개
    </div>
  </div>
</div>
