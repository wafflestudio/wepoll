<div class="link-excerpt">
    <!-- TODO : 링크걸어주세요 -->
    <div class="title"><%= empty ? (raw "<span style=\"color:#888\">링크가 없습니다.</span>") : (link_to link.title.truncate(40), '#') %></div>
  <div class="meta-info clearfix">
    <div class="support-count">공감 <%= empty ? 0 : link.like%></div>
    <!-- TODO : 링크걸어주세요 -->
    <div class="nickname"><%= empty ? "위폴" : (link_to link.user.nickname, '#')%></div>
  </div>
</div>
